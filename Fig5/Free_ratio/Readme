#  Free-ratio test was used to estimate the dN/dS ratio for each branch independently. 
#The Free_ratio_model.sh pipeline was used to run codeml and compile the results, with all parameter settings specified in the codeml control file (demo.ctl).
